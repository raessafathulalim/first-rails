<div class="row">
	<% @posts.each do |post| %>
	<div class="col-md-4 well">
		<h2><%= post.title %></h2>
		<p><%= post.body %> </p>
		<%= link_to("View Details ...", post_path(post), {
			:class => 'btn btn-secondary'
		}) %>
	</div>
	<% end %>
</div>
